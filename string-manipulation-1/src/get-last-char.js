/* exported getLastChar */
function getLastChar(string) {
  var last = string[string.length - 1];
  return last;
}
/*
1. define a variable last
2.store the value of the last string in that variable
3.return the variable last
*/
