/* exported isLowerCased */
function isLowerCased(word) {
  var boolean = false;
  if (word === word.toLowerCase()) {
    boolean = true;
  }
  return boolean;
}
/*
1. variable that stores a false boolean value
2. conditinal if checking if the string is equal to the string but in capitals
3.assign a true boolean to the varibale boolean if it is
return the variable boolean
*/
