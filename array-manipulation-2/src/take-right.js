/* exported takeRight */
/* function take(array, count) {
  var container = [];
  if (array.length > 0) {
    for (var i = 0; i < count; i++) {
      container.push(array[i]);
    }
  }
  return container;
}
*/
/*
1. define a storage variable for the new array.
2.a conditional statement checking if the array has a length greater than 0
3.for loop inside if statement codeblock  that checks if count is greater than
zero increment
*/
